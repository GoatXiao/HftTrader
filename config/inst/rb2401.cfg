{
    inst = "rb2401";
    inst_list = ["hc2401"]; // 关联合约列表
    param_path = "config/param/rb2401.cfg"; // 参数文件
    exchange_rebate_rate = 0.4; // 交易所返佣比例
    offset_type_mode = 0; // 开平仓模式：0/1/2/3
    max_inventory = 1000; // 最大允许持仓（今仓）
    max_volume = 10000; // 最大允许成交量
    max_cancel = 480; // 最大允许撤单数
    max_net_pos = 1; // 最大允许净头存
}
